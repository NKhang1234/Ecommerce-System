spring:
  config:
    import: "optional:file:.env[.properties]"
  application:
    name: store
#  datasource:
#    url: jdbc:mysql://localhost:3306/store_api?createDatabaseIfNotExist=true
#    username: root
#    password: MyPassword!
#  jpa:
#    show-sql: true
  datasource:
    url: jdbc:postgresql://ep-royal-band-a1p32bil-pooler.ap-southeast-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_8MNivIPlt3rp&sslmode=require&channelBinding=require
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 5
      data-source-properties:
        ssl: true
        sslmode: require

  jpa:
    hibernate:
      ddl-auto: update   # use validate in production
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 7200 # 2 hr
    refreshTokenExpiration: 604800 # 7 days

stripe:
  secretKey: ${STRIPE_SECRET_KEY}
  webhookSecretKey: ${STRIPE_WEBHOOK_SECRET_KEY}

websiteUrl: http://localhost:3000